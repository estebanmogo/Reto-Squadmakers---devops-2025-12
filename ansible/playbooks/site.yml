---
- name: Deploy IoT telemetry stack
  hosts: iot_nodes
  become: "{{ ansible_become | default(true) }}"
  vars:
    ansible_python_interpreter: /usr/bin/python3
  roles:
    - docker
    - kafka
    - clickhouse
    - thingsboard
    - grafana
